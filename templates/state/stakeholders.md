# Stakeholder Register

<!--
Agent: Populate this during seed or early Phase One. Ask for stakeholder names
and roles — a single role may have multiple people. Once captured, use names
instead of generic role labels throughout the plan (e.g., sign-off, branch
ownership, Kanban tasks).

If the developer is the sole stakeholder and doesn't want to list names,
"Developer" as a single entry is acceptable.
-->

| Name | Role | Contact | Notes |
|------|------|---------|-------|
| | | | |
| | | | |

**Roles (examples):** `Developer` · `Client` · `Project Lead` · `Designer` · `QA` · `Stakeholder`

**Guidelines:**
- Capture during seed or early Phase One. This replaces generic role references
  throughout the plan (e.g., "Alice Chen signs off" instead of "Developer signs off").
- A single role may have multiple names. List each person on their own row.
- If the developer prefers not to name individuals, a single "Developer" entry
  with no contact details is fine — generic role labels continue to apply.
- Update if stakeholders change during the project.
